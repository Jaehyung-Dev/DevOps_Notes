1.변수의 선언 : 자료형 + 변수명
int likeCnt; // 좋아요 개수를 저장하는 정수형 변수 생성
float pi = 3.14 // PI(3.14)를 저장하는 실수형 변수 생성
boolean doStudy = true; // true, false 값을 저장하는 boolean 변수 생성

2.변수 선언 시 유의사항
(1). 변수명 첫 글자는 문자, $, _ 만 사용가능
(2). 두 글자 이상이 합쳐진 변수명을 사용할 때 카멜케이스 표기법 사용

3.변수의 사용
(1). 변수에 값을 최초로 저장(할당)하는 것을 초기화라고 한다.
(2). 변수에 저장되어있는 값은 변수명으로 사용 가능
(3). 변수는 값의 재할당이 가능
float pi = 3.13; // PI 변수에 3.13으로 값을 잘 못 초기화 했을때
pi = 3.14; // 이런식으로 값의 재할당이 가능하다.
(4). 변수를 초기화 하지 않고 사용하면(The local variable errorVal may not have been initialized)에러가 발생

4.클래스 변수 : 클래스 내에 static으로 선언된 변수로 클래스 중괄호 안에서 변수에 접근, 사용, 저장 가능
static int num; // 정수형 클래스 변수 생성

5.인스턴스 변수 : static 키워드 없이 클래스 내에서 선언된 변수로 클래스의 인스턴스가 생성될 때 생성
int num2; // 정수형 인스턴스 변수 생성

6.지역 변수 : 메소드나 생성자 블럭 내부에서 선언된 변수로 해당 블럭 범위 외에서는 변수 사용이 불가능
if(true) {
    int num3 = 10;
}
System.out.println(num3); // 제어문의 지역변수 i는 제어문 밖에서 사용이 불가능하기 때문에 에러 발생

7.char 변수 : 문자 1개를 담는 자료형
char ch1 = 'A'; // 직접 문자 1개를 담거나
char ch2 = 65; // 문자의 유니코드 환산 값으로도 담을수 있다.

8.var 변수 : Java의 자료형 추론 기능으로 자동으로 타입을 지정해주는 자료형(주로 연산시에 결과의 타입을 확신할 수 없는 경우 사용한다 함)
var str = "hello"; // str 변수가 자동으로 String으로 타입 지정
var iNum; // var 변수는 선언과 동시에 초기화 되지 않으면 에러 발생

9.상수 : 상수는 한 번 지정하면 변경할 수 없는 값으로 final 키워드를 이용해서 선언한다.
final int MAX_STUDY_HOUR = 24; // 상수는 모든 변수명 대문자 + 스네이크표기법을 사용
final double PI; // 먼저 선언하고
PI = 3.14;  // 나중에 초기화 가능;

10.형변환 : byte 크기가 작은 타입에서 큰 타입으로 변경하거나, (정수 -> 실수)로 세밀한 표현범위로 변경하는 경우 자동 형변환이 일어나며 반대의 경우에는 강제 형변환을 해야한다.
float fNum = 3.14f;
int iNum = (int)fNum; // 강제 형변환할 타입으로 묶어줘야 한다.
